import React from "react";
import Container from "../components/Container";

const Header = () => {
  return (
    <header>
      <Container content={<p>Header</p>} />
    </header>
  );
};

export default Header;
